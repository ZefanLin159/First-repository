# servlet Config
server:
  servlet:
    context-path: /community
  #    session:
  #      cookie:
  #        secure: off
  port: 8080
# thymeleaf config
spring:
  thymeleaf:
    cache: false
    prefix: classpath:/templates/
    suffix: .html
  #    servlet:
  #      content-type: text/html
  #    encoding: UTF-8
  #    mode: HTML

  #    spring.mvc.static-path-pattern=/**
  #    spring.resources.static-locations=classpath:/static/
  # DataSourceProperties
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    type: com.zaxxer.hikari.HikariDataSource
    hikari:
      maximum-pool-size: 15
      minimum-idle: 5
      # connection-timeJavaMailSender' that could not be found.out: 30000
    url: jdbc:mysql://localhost:3306/community?characterEncoding=utf-8&useSSL=false&serverTimezone=Hongkong
  #    MailProperties
  mail:
    default-encoding: UTF-8
    host: smtp.qq.com
    port: 465
    username: 915654746@qq.com
    password: akuuxlkwmjghbffi
    protocol: smtps
    properties: # mail.smtp.ssl.enable=true
      mail:
        auth: true
        starttls:
          enable: false
          required: false
  # redis配置
  redis:
    database: 11
    host: localhost
    port: 6379
    #kafka配置
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: community-consumer-group
      enable-auto-commit: true
      # 3s
      auto-commit-interval: 3000
  data:
    elasticsearch:

    # 集群已废弃
    #       cluster-name: nowcoder
    #       cluster-nodes: 127.0.0.1:9300
  # task
  task:
    execution:
      pool:
        core-size: 5 #线程池线程总数
        max-size: 15 # 线程数自动扩容
        queue-capacity: 100 # 队列缓冲任务数
    scheduling:
      pool:
        size: 5 # 线程池线程总数
  quartz:
    job-store-type: jdbc
    scheduler-name: communityScheduler



# mybatis 配置
mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.newcorder.community.entity
  configuration:
    # 启动自增长主键
    use-generated-keys: true
    # 自动匹配驼峰和下划线命名方式
    map-underscore-to-camel-case: true
# logger
logging:
  level:
    com:
      newcorder:
        community: debug # 这个包级别是debug
  file:
    path: d:/work/data/newcoder/community.log

# community(diy)
community:
  path:
    domain: http://localhost:8080
    upload: d:/work/data/upload

# qiniu
qiniu:
  key:
    access: j8LDB2_YixXW6bp9PTmwwfJo43QS7SmMo_AMio7_
    secret: xRTLtPvR0EsiZRPZmJS_BbBj-hok87EQK-TAnqhS
  bucket:
    header:
      name: communitylinheader
      url: http://rcfwrfcmn.hn-bkt.clouddn.com
#share先不做，因为7,8节还没做
